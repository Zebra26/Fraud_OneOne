"""
Security utilities package for JWT, HMAC signing, RBAC, and model integrity helpers.

This file enables importing submodules via `backend.security.*` when running
tools and scripts from the repo root (e.g., `scripts/send_synthetic_scores.py`).
"""

