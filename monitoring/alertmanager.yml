route:
  receiver: dev-null
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: dev-null
    webhook_configs: []

# Example (uncomment and configure):
#  - name: slack
#    slack_configs:
#      - send_resolved: true
#        api_url: https://hooks.slack.com/services/XXX/YYY/ZZZ
#        channel: "#alerts"
